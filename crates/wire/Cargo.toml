[package]
name = "ratatosk-wire"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Формат кадра Ratatosk v0.1: заголовок, классы размера, паддинг (§5.5, §7)"

# Зависимостей нет и не должно появиться. Этот крейт описывает байты на проводе;
# он обязан собираться и фаззиться без криптостека и без рантайма.
[dependencies]

[dev-dependencies]

[lints.rust]
missing_docs = "warn"
unsafe_code = "forbid"
